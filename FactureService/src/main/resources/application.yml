server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/centre_dentaire_facturation
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: facture-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
