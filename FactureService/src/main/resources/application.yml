server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/facturedb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: appointment-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
